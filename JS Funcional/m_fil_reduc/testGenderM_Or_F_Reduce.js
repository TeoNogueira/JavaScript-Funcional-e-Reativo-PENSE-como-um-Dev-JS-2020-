
// 1 - Declare uma mensagem no começo da frase indique masc or feminino do primeiro array ex:
// a acerola é boa, o sapoti é bom; <<< Resolvido;

// 2 - compare as frutas semelhantes entre os dois arrays:

//[ 'sapoti', 'siriguela', 'cajá', 'graviola', 'acerola', 'laranja' ] <<<< Resolvido!!

let sucosInMarket1 = ['banana', 'maça', 'uva', 'pêra', 'manga', 'sapoti', 'siriguela', 'cajá', 'graviola', 'acerola', 'laranja'];



let sucosInMarket2 = ['Jaca', 'mamão', 'limão', 'pitanga', 'melão', 'sapoti', 'siriguela', 'cajá', 'graviola', 'acerola', 'laranja'];

// const listResult = sucosInMarket1.reduce((acc, item, index, array) => {

// const GenderMascOrFemale = /a$/.test(item) ? 'A' : 'O';
// const finalPhraseGenders = /a$/.test(item) ? 'é boa!' : 'é bom';


// if(index === array.length -1) {

    
//     return acc + `${GenderMascOrFemale} ${item} ${finalPhraseGenders}.`

// }

// return acc + `${GenderMascOrFemale} ${item} ${finalPhraseGenders},\n `

// }, 'Lista de Frutas Mercado 1:\n');

// console.log(listResult);


// const compareMercados = sucosInMarket1.filter((coisasSemelhantes) => sucosInMarket2.includes(coisasSemelhantes));


// console.log(compareMercados);

